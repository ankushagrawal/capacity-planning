apply plugin: 'java'

sourceCompatibility = 1.8
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'gov.nist.math', name: 'jama', version: '1.0.2' //used for matrix solver
    testCompile group: 'junit', name: 'junit', version: '4.11'
}